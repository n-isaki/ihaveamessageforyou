rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Public Read Access (Needed for streaming audio)
      allow read: if true;
      
      // Write/Delete only for Authenticated Admins
      allow write: if request.auth != null;
    }
  }
}
